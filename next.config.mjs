/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "via.placeholder.com",
            "daoucables.s3.eu-north-1.amazonaws.com",
            "http://www.w3.org",
            "i.postimg.cc"
        ],
    },
};

export default nextConfig;
